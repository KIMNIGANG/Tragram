<%= link_to "instagramAuth", "https://api.instagram.com/oauth/authorize?client_id=555005406003631&redirect_uri=https://f9fb-133-51-117-147.jp.ngrok.io/authpass&scope=user_profile,user_media&response_type=code"%>

<%# require 'net/http'
require 'uri'

uri = URI.parse("https://api.instagram.com/oauth/access_token")
request = Net::HTTP::Post.new(uri)

req_options = {
  use_ssl: uri.scheme == "https",
}

response = Net::HTTP.start(uri.hostname, uri.port, req_options) do |http|
  http.request(request)
end

# response.code
# response.body  %>
